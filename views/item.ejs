<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="shortcut icon" type="x-icon" href="../images/4151901.png">
    <link rel="stylesheet" href="../css/item.css">
    <link rel="stylesheet" href="https://kit.fontawesome.com/e390cab89c.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"/>
    <script src="https://kit.fontawesome.com/e390cab89c.js" crossorigin="anonymous"></script>
    <title>item</title>
    <script src="../js/user.js"></script>
</head>
<body>
        
  <%- include("./partials/header.ejs") %>
  <%- include("./partials/chat.ejs") %>
  <%- include("./partials/tran.ejs") %>

    
          <div class="product-container">
            <img src="../images/<%=item.image%>" alt="Product Photo">
            <div class="product-info">
              <h2 class="product-title"><%=item.title%></h2>
              <p class="product-description"><%=item.description%></p>
              
                <%if( item.hasoffer==="true"){%>
                  <%let newprice=item.Price-item.Price*(item.offervalue/100)%>
                  <br>
                  <p class="product-price"><s style="color: red;">$<%=item.Price%></s><br>$<%=newprice%></p>
                  <br>
              <%}else{%>

              <p class="product-price">$<%=item.Price%></p>
                <%}%>

              <% if(user==="" ) {%>

                <a href="/sign"><button class="checkout-button">Checkout</button></a>

                <% }else { 
                  if(user.Type==="user")
                  {
                  %>
                  <a href="/add/<%=item.id%>"><button class="checkout-button">Checkout</button></a>
                  <% } %>
                  <% } %>
             
            </div>
          </div>
          <div class="center">
          <%- include("./partials/review.ejs") %>
          </div>

          <%- include("./partials/footer.ejs") %>




    
</body>
</html>