function dashboard()
{
    const element = document.getElementById('dashboard').classList.add('active');

    // Add the 'active' class to the element
    element.classList.add('active');
}
function customers()
{
     document.getElementById('customers').classList.add('active');

    // Add the 'active' class to the element
    
}
function orders()
{
    const element = document.getElementById('orders').classList.add('active');

    // Add the 'active' class to the element
    element.classList.add('active');
}
function Inventory()
{
    const element = document.getElementById('Inventory').classList.add('active');

    // Add the 'active' class to the element
    element.classList.add('active');
}
function Task()
{
    const element = document.getElementById('Task').classList.add('active');

    // Add the 'active' class to the element
    element.classList.add('active');
}
function offers()
{
    const element = document.getElementById('offers').classList.add('active');

    // Add the 'active' class to the element
    element.classList.add('active');
}
function addUser()
{
    const element = document.getElementById('addUser').classList.add('active');

    // Add the 'active' class to the element
    element.classList.add('active');
}
function edituser()
{
    const element = document.getElementById('edituser').classList.add('active');

    // Add the 'active' class to the element
    element.classList.add('active');
}